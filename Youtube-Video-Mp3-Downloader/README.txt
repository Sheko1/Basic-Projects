Libraries used:

pytube
moviepy

How to install?:
open cmd and type:

pip install moviepy
pip install pytube - if that not worked install git and try:
pip install git+https://github.com/nficano/pytube - if you have installed git